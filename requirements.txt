streamlit>=1.37
numpy>=1.26
sympy>=1.12
plotly>=5.20

# Usado em componentes.matriz_input (rótulos de linha/coluna no editor).
# Fixado a <3: observámos um AttributeError intermitente ("partially
# initialized module 'pandas'... circular import") quando o pandas 3.0.x
# é importado durante reruns rápidos do Streamlit (navegação em sucessão).
pandas<3
